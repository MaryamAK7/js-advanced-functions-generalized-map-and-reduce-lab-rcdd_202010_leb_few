// Add your functions here

function map(arr, fct){
  let arr2=[]
  for(let i=0;i <arr.length;i++){
    arr2.push(fct(arr[i]));
  }
  return arr2;
}

function reduce(arr, fct, start){
 let r= ()
 
 
}